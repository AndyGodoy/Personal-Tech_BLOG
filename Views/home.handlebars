{{> partials/navbar}}

<h1>Welcome to the Blog Site</h1>

{{#each posts}}
  <div class="post">
    <h2>{{this.title}}</h2>
    <p>{{this.content}}</p>
    <p>By {{this.user.username}}</p>
    <a href="/post/{{this.id}}">Read More</a>
  </div>
{{/each}}
